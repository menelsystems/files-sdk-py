# Gitignored files copied into every new worktree by Claude Code.
# Uses .gitignore syntax; only files that are ALSO gitignored are copied,
# so tracked files are never duplicated. See:
# https://code.claude.com/docs/en/worktrees#copy-gitignored-files-into-worktrees
.secrets
