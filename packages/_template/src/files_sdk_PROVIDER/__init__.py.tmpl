"""files-sdk-<provider> — stub adapter."""

from typing import TYPE_CHECKING

from .adapter import <PROVIDER>Adapter

__version__ = "0.0.0"
__all__ = ["<PROVIDER>Adapter"]

if TYPE_CHECKING:
    # Tripwire: pyright structurally compares the impl against the Protocol
    # at type-check time. Add an _async_check line too once an async impl is
    # introduced. See files_sdk_s3.__init__ for full rationale.
    from files_sdk.adapter import Adapter

    _sync_check: type[Adapter] = <PROVIDER>Adapter
